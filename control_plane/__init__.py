"""Engineering control plane: a governed, stateful orchestration layer that
converts requirements into reviewable repository changes.

The URL shortener under ``shortener/`` is the proving workload; this package
is the system under evaluation.
"""

__version__ = "0.1.0"
